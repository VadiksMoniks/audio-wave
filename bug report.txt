есть проблема с flac. при переключении назад через кнопку при включении .flac - приложение вылетает.//ПОКА УБРАЛ .flac, чтобы такие файлы не отображались
при шафле треков часто выпадает один и тот же, либо, предыдущий включаеться после текущего. нужно поменять рандом. добавить номер текущего, и предыдущего, и проверять их
создать какой-то класс-хелпер, и туда вынести код, который обрезает строку в файлах Player.cpp(get_track_name, get_track_artist) и PlayerQueue(конструктор)
возможно, немного переработать код для обрезания строки, чтобы он обрезал не всегда до 25 символов, а до n, которые будут переданы в функцию
возможно, добавить градиент, если есть картинка в треке
попробовать цвета поменять на кнопках
что-то придумать с кнопкой паузы, а то иконка там черная
подумать над громкостью